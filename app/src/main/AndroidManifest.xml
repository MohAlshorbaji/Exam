<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Activites.Steptwo"></activity>
        <activity android:name=".Activities.Activites.Drawerlayout" />
        <activity android:name=".ui.login.Drawerlayou" />
        <activity
            android:name=".ui.login.Drawerlayout"
            android:label="@string/title_activity_drawerlayout" />
        <activity android:name=".Activities.Activites.Main" />
        <activity android:name=".Activities.Activites.Steps" />
        <activity android:name=".Activities.Activites.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>